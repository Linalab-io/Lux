Task 4 blocker cycle evidence

Cycle behavior implemented through explicit blocker graph path checks before adding a blocker relationship.

Relevant passing tests:
- lux_verification_quarantines_blocker_cycle
- task_dag_rejects_explicit_cycle_without_fallback_ordering

Observed outcome:
- A proposed A -> B -> A blocker cycle is rejected.
- RunState transitions to Quarantined.
- RunState.last_error is blocker_cycle_detected.
- TaskDAG topological_ids_checked returns an explicit cycle error instead of falling back to arbitrary ordering.
