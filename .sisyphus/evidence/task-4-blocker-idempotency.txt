Task 4 blocker idempotency evidence

Stable blocker IDs now derive from check category + check name + spec_ref via sha256.
create_or_update_blocker reuses and updates an existing same-key blocker instead of creating duplicate tickets.

Relevant passing tests:
- lux_verification_repeated_same_failure_converges_to_one_blocker
- lux_verification_quarantines_after_blocker_attempt_limit

Observed outcome:
- Three repeated failures for the same verification check return the same blocker ticket ID.
- Ticket store contains exactly one blocker ticket for the repeated same check.
- Fourth attempt for the same blocker exceeds max_blocker_attempts_per_ticket=3 and quarantines with blocker_escalation_required.
